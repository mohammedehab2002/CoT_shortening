seed: 42

loss:
  type: "optimal_stopping"
  lam: 0.0001
  normalize: false

length_thresh: 10000

lr: 1e-5
batch_size: 1
gradient_accumulation_steps: 100
epochs: 10

num_finetuning_layers: 2

initialization: "random"
save_total_limit: 1

checkpoint_dir: "/home/ehab02/orcd/pool/checkpoints"